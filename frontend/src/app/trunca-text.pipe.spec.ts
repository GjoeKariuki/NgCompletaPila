import { TruncaTextPipe } from './trunca-text.pipe';

describe('TruncaTextPipe', () => {
  it('create an instance', () => {
    const pipe = new TruncaTextPipe();
    expect(pipe).toBeTruthy();
  });
});
